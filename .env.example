CRON_SECRET=replace-with-a-long-random-secret

# Optional but recommended for Vercel deployment so cache/history persist online.
# Create a Vercel Blob store and connect it to the project.
BLOB_READ_WRITE_TOKEN=

# Optional: used by absolute API links in docs or scripts.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
